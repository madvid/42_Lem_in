# Results of the tests and performance:

First of all, scripts in th directory **Tests_bash** are performed.
There is several scripts:
* *check_invalid.sh*
* *check_valid.sh*
* *checker.sh*
* *comparator.sh*

##